import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  mainStack: {
    width: "23rem",
    padding: "0 2rem",
    height: "90vh",
    margin: "0 auto",
  },
  containerStack: {
    width: "100%"
  },
}));